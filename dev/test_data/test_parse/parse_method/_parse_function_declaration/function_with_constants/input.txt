FUNCTION validateRange(value : REAL) : BOOLEAN;
    CONSTANT
        minValue : REAL := 0.0;
        maxValue : REAL := 100.0;
    END_CONSTANT;

    IF (value >= minValue) AND (value <= maxValue) THEN
        RETURN(TRUE);
    ELSE
        RETURN(FALSE);
    END_IF;
END_FUNCTION;